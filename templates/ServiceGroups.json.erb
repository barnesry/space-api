{
	"SERVICE_GROUPS": {
<% service_groups.each do | service | %>		
        "<%= service.name %>": {
            "MEMBERS": [<% service.members.each do |member| %>"<%= member.name %>",<% end %> ], 
            "GROUPS": [],
            "DESCRIPTION": "<%= service.description %>"
		},
<% end %>
    }
}